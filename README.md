To run locally:
1. Launch virtual enviornment: python3 -m venv .venv
2. Active virtual enviornment: source .venv/bin/activate
3. Install packages: pip install -r requirements.txt
4. Run streamlit app: streamlit run app.py
